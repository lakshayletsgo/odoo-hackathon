// Fix any undefined component imports in the dashboard
// Make sure all components are properly exported

// Check if UserAvatar is properly exported
export { UserAvatar } from "./user-avatar";

// Check if ProfilePictureUpload is properly exported
export { ProfilePictureUpload } from "./profile-picture-upload";
